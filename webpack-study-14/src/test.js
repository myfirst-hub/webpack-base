let sum = (a, b) => {
  return a + b + 'sum';
}

let minus = (a, b) => {
  return a - b + 'minus';
}

export let minus1 = (a, b) => {
  return a - b + 'minus1';
}

export default {
  sum,
  minus,
}